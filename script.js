// script.js - fallback / util para tienda (usa Realtime DB)
document.addEventListener('DOMContentLoaded', () => {
  console.log('Inicializando tienda (realtime)');

  // Si ya incluíste la lógica en index.html, este archivo es opcional.
  // Lo dejo corto: escucha la ruta /config y dispara evento para actualizar UI.
});